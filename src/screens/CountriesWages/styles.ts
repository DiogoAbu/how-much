import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  itemRight: {
    alignSelf: 'center',
  },
  itemTitleEmpty: {
    fontStyle: 'italic',
  },
});

export default styles;
